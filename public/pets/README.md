Current transparent plush pet images:

- teddy-plush-v2.webp
- golden-plush-v2.webp
- ragdoll-plush-v2.webp

These files are loaded with `import.meta.env.BASE_URL`, so they work under the GitHub Pages `/yy-station/` base path.

The older `teddy.webp`, `golden.webp`, and `ragdoll.webp` files are retained as source references and are no longer loaded by the pet widget.
